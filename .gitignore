# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
build/
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor and OS
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
!.env.example
# Build-mode selector for the user-study build (no secrets — needed by build:study).
!.env.study

# Test / coverage
coverage/
*.lcov

# Vite cache
.vite/

# Claude Code working files (project-local, not for the repo)
.claude/
CLAUDE_CODE_PROMPTS.md
.vercel
server/__pycache__/
# Local LLM outline cache (full build only; regenerated on demand, not frozen).
server/.outline_cache/
